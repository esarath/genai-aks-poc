# ── RAG API — Lightweight POC stack ──────────────────────────────────────────
# All packages chosen for zero-cost local/AKS execution
# Production equivalents noted inline

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Vector DB — ChromaDB (POC, runs in-process or as single container)
# PRODUCTION: Qdrant (qdrant-client==1.9.0) or Weaviate or Pinecone
chromadb==0.5.3

# Embeddings — lightweight 384-dim model (~90MB, CPU-only)
# PRODUCTION: text-embedding-3-small (Azure OpenAI) or cohere-embed-v3
sentence-transformers==3.0.1

# LLM client — calls local Ollama (phi3:mini ~2GB) or any OpenAI-compatible API
# PRODUCTION: azure-openai, anthropic, or cohere SDK
litellm==1.44.0
httpx==0.27.2

# Data validation
pydantic==2.8.2

# Prometheus metrics
prometheus-client==0.20.0

# Async support
anyio==4.4.0

# ── Not used in POC, referenced in production guide ──────────────────────────
# qdrant-client==1.9.0        # Qdrant production vector DB
# langchain==0.2.12           # Full LangChain orchestration
# openai==1.40.0              # Azure OpenAI / OpenAI API
